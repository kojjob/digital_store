class CreateWishlistItems < ActiveRecord::Migration[7.0]
  # Migration is intentionally empty because the table already exists
  def change
    # This migration is intentionally empty because the table already exists
    # We're keeping this file to maintain migration versioning
    say "The wishlist_items table already exists - skipping"
  end
end
